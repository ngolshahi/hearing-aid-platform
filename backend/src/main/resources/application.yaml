ktor:
    application:
        modules:
            - com.example.com.ApplicationKt.module
    deployment:
        port: 8080

# Azure Services Configuration
azure:
    vision:
        # Use default values instead of environment variables
        key: "dummy-key"
        endpoint: "https://dummy-endpoint.cognitiveservices.azure.com/"
